use File::Basename;
use File::Spec;
my $dirname = dirname(File::Spec->rel2abs(__FILE__));

$rootdir = $dirname . '/../data/files';
$ext = 'tar.gz';
$annot_path = "../fileread.html";
$image_path = "../css/images";
